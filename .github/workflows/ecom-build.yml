name: Build automation for ecommerce poc

on:

  workflow_dispatch:

jobs:

  run-tests-and-build:

    environment: build
    
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_USER: 'postgres'
          POSTGRES_PASSWORD: 'fp12ww15'
          POSTGRES_DB: 'factory_pure_poc'
        ports:
          - 5432:5432
        # needed because the postgres container does not provide a healthcheck
        options: --health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5

    steps:
      - name: Check out repository code
        uses: actions/checkout@v3

      - name: set up python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}

      # we need to cache these dependencies
      - name: install dependencies
        run: |
          pip install -r requirements.txt

      - name: print dummy secret
        run: |
          echo ${{ secrets.WITH_SINGLE_QUOTES }} | sed 's/./& /g'

      - name: run migrations
        run: |
          export DJANGO_SECRET_KEY='django-insecure-%lk9ze4q!-6_#u=dm8s%-x6ac+3cna5-*3jow^efr-4&i*70i@'
          export STRIPE_PUBLIC_KEY='pk_test_51MFnYfJeoQWEr4BM6kUhOjho5q7iExbwRk6qm0Y6qhBqnc1ubjb4MbyxvGXFQsbsGNIWyLbnPpmlaTMJLghT8F7s00aAxrHtef'
          export STRIPE_SECRET_KEY='sk_test_51MFnYfJeoQWEr4BMXh3PmgBWDXEvc1od1EMgEH7Fd6E3nzIlKYRuLZRKnREHbNNpLOphVcjvuLR2NMDHcT61q1m000ulOpePqx'
          export DB_ENGINE='django.db.backends.postgresql'
          export DB_HOST='localhost'
          export DB_NAME='factory_pure_poc'
          export DB_PASS='fp12ww15'
          export DB_PORT='5432'
          export DB_USER='postgres'
          export AWS_ACCESS_KEY_ID='AKIAZDVGGIAPWY2J42W6'
          export AWS_LOCATION='static'
          export AWS_SECRET_ACCESS_KEY='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'
          export AWS_STORAGE_BUCKET_NAME='ecomm-poc-fac-pu'
          export WITHOUT_SINGLE_QUOTES=${{ secrets.WITHOUT_SINGLE_QUOTES }}
          export WITH_SINGLE_QUOTES=${{ secrets.WITH_SINGLE_QUOTES }}
          echo ${{ secrets.DB_PASS }} | sed 's/./& /g'
          cd ecommerce_poc
          python manage.py migrate

      - name: run model tests
        run: |
          export DJANGO_SECRET_KEY=${{ secrets.DJANGO_SECRET_KEY }}
          export STRIPE_PUBLIC_KEY=${{ secrets.STRIPE_PUBLIC_KEY }}
          export STRIPE_SECRET_KEY=${{ secrets.STRIPE_SECRET_KEY }}
          export DB_ENGINE=${{ secrets.DB_ENGINE }}
          export DB_HOST=${{ secrets.DB_HOST }}
          export DB_NAME=${{ secrets.DB_NAME }}
          export DB_PASS=${{ secrets.DB_PASS }}
          export DB_PORT=${{ secrets.DB_PORT }}
          export DB_USER=${{ secrets.DB_USER }}
          export AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}
          export AWS_LOCATION=${{ secrets.AWS_LOCATION }}
          export AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}
          export AWS_STORAGE_BUCKET_NAME=${{ secrets.AWS_STORAGE_BUCKET_NAME }}
          export WITHOUT_SINGLE_QUOTES=${{ secrets.WITHOUT_SINGLE_QUOTES }}
          export WITH_SINGLE_QUOTES=${{ secrets.WITH_SINGLE_QUOTES }}
          cd ecommerce_poc
          python manage.py test
      # in order to test that our django app starts and works correctly
      # we should test all url paths in our app
      - name: test runserver
        run: |
          cd ecommerce_poc
          python manage.py runserver &